#include "main.h"
/**
 * _abc - computes the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value of int
 */
int _abc(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
